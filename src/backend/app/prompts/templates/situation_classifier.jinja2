Classify the user's message into one interaction situation.
Output JSON only.

Taxonomy (pick exactly one):
{{ situation_taxonomy }}

User message:
{{ user_message }}

Recent context:
{{ recent_context }}

Output:
{"situation": "...", "confidence": 0.0}
